
一、定时短信功能接口：（发送对象、[群发类型]、短信类型、定时时间、短信内容、发送人ID，地区信息等）
   
   1）个性化发送（指定单个或多个号码的设置定时短信）     --->  学生的学籍编号（或学生ID）
   2）群发短信（以班级为单位、或学校为单位设置定时短信） --->  学校ID、班级ID（年级ID）
     


   修改点：
   UniteSendTread（目前只提供 学生ID 参数 ）--> 变更为 同时 支持以班级（或年级）为单位。
        ||
        || 
        ||
   MessageUtiltity2.handleMulitMessage (  ) 这个方法要重载  支持发送对象的类型为 个人、班级等，对应其对象的类型。
      


二、Groupsend 定时任务的处理方式修改
   1、采用新功能接口所产生的任务标识，这样的任务记录将采用新的处理方式(嵌入过滤未订购业务的号码逻辑，
生成最终的待发对象信息)去处理短信。
   2、针对新的方式建立的任务，对任务的处理增加提前量的处理，定时发送前 完成发送对象的封装逻辑。


